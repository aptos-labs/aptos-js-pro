---
"@aptos-labs/js-pro": major
"@aptos-labs/react": major
---

Make published packages ESM-only with `"type": "module"`, remove CommonJS `require` export conditions, and replace tsup with native `tsc` builds.
